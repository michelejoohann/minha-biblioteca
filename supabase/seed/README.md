# Seeds

O Supabase executa `../seed.sql` depois das migrações. Os cinco proprietários familiares também são criados automaticamente pelo trigger `on_household_created`, de modo que ambientes reais não dependem do seed local.

